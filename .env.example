# Database Configuration
DB_USER=postgres
DB_PASSWORD=your_postgres_password_here

# Leave as localhost for local development
DB_HOST=localhost
DB_PORT=5432

# The name of your local PostgreSQL database
DB_NAME=devcollab_db

# JWT Authentication Secret (Use a strong random string in production)
JWT_SECRET=devcollab_secret
